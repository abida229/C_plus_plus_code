#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double x, result;
    cout<<"Enter value: ";
    cin>>x;
    result = abs(x);
    if x is less than zero;
     cout << "absolute value of (" << x << ") = |" << x << "| = " << result << endl;
    else(x is greater than zero  )
    cout<<"This is already absolute";
    
    return 0;
}
